import React from 'react'; import {ShieldCheck} from 'lucide-react'; import {UserProfile,GoldSpotData} from '../types';
export function Header({currentUser,allUsers,onSelectUser,activeTab,setActiveTab,spotData}:{currentUser:UserProfile;allUsers:UserProfile[];onSelectUser:(id:string)=>void;activeTab:string;setActiveTab:(t:any)=>void;spotData:GoldSpotData}){return <header className="sticky top-0 z-20 bg-[#050505]/95 backdrop-blur border-b border-[#d4af3722] px-4 sm:px-8 py-4"><div className="max-w-7xl mx-auto flex flex-wrap items-center gap-4 justify-between"><div className="font-serif text-white font-bold tracking-widest text-xl">GOLD<span className="text-[#d4af37]">10</span></div><nav className="flex flex-wrap gap-1">{[['dashboard','Dashboard'],['purchase','Buy'],['blockchain','Blockchain'],['architecture','Security']].map(([id,label])=><button key={id} onClick={()=>setActiveTab(id)} className={`px-3 py-2 text-[10px] uppercase tracking-widest rounded ${activeTab===id?'bg-[#d4af37] text-black':'text-[#d4af37] hover:bg-[#d4af3715]'}`}>{label}</button>)}</nav><div className="flex items-center gap-3 text-xs"><span className="text-zinc-400">${spotData.pricePerGram.toFixed(2)}/g</span><select value={currentUser.userId} onChange={e=>onSelectUser(e.target.value)} className="bg-[#0a0a0a] border border-[#d4af3733] text-white p-2 rounded">{allUsers.map(u=><option key={u.userId} value={u.userId}>{u.name}</option>)}</select></div></div></header>}
