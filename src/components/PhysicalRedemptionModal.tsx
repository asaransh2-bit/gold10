import React,{useState} from 'react'; import {UserProfile,VaultBar} from '../types'; export function PhysicalRedemptionModal({currentUser,userBars,onClose,onRedeemSuccess}:{currentUser:UserProfile;userBars:VaultBar[];onClose:()=>void;onRedeemSuccess:(d:any)=>void}){const [amount,setAmount]=useState(1);return <div className="fixed inset-0 z-50 bg-black/80 grid place-items-center p-4"><div className="bg-[#0a0a0a] border border-[#d4af37] p-6 rounded w-full max-w-md"><h2 className="font-serif text-white text-xl">Physical Redemption — Demo</h2><p className="text-xs text-zinc-400 mt-2">Simulates a redemption request. No physical shipment is created.</p><input type="number" min="1" max={Math.floor(currentUser.goldBalance)} value={amount} onChange={e=>setAmount(+e.target.value)} className="w-full mt-4 bg-black border border-[#d4af3733] p-3 text-white"/><div className="text-xs text-zinc-500 mt-2">Available demo bars: {userBars.length}</div><div className="flex gap-2 mt-5"><button onClick={()=>{onRedeemSuccess({success:true});onClose()}} className="flex-1 bg-[#d4af37] text-black p-3 font-bold">Create Demo Request</button><button onClick={onClose} className="px-4 border border-zinc-700 text-white">Cancel</button></div></div></div>}
